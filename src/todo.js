export default class Todo {

    constructor(title, dueDate = null, priority = "low") {

        this.title = title;
        this.dueDate = dueDate;
        tihs.priority = priority;
        this.completed = false;
    
    }

}
